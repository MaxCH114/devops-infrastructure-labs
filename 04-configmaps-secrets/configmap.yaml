apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: demo-app
data:
  APP_COLOR: "blue"
  APP_MODE: "production"
  app.properties: |
    database.connection.timeout=30
    logging.level=INFO
    feature.flag.new_ui=true
